create table Booking(
Id int primary key,
Name varchar(30),
Age int,
Gender varchar(10),
City varchar(30),
Schedule datetime
);
/*IDENTITY*/
create table Account(
Id int primary key,
username varchar(20),
password varchar(20)
);

